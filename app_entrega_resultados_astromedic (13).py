
# (contenido ya visible en el canvas, reemplazado anteriormente)
# Aquí solo estamos reescribiendo el archivo para descarga

# IMPORTANTE: ya contiene el cambio de .encode("utf-8", errors="replace")
